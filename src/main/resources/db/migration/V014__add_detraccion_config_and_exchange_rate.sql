-- V014: NOOP — exchange_rate ya creada en V002 con company_id.
--        Los INSERT INTO configuration requieren company_id (NOT NULL);
--        se siembran al crear cada empresa desde CompanyServiceImpl.
SELECT 1;
